export type SkillScore = {
  skill: string;
  score: number;
};

export type ProfessionData = {
  testLink?: string;
  professions?: string[];
  groups?: string[];
  image?: string;
  backgroundColor?: string;
  
  reason?: string;
  description?: string;
};
